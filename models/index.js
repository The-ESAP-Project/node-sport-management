const User = require('./UserModel');
const StudentInfo = require('./StudentInfoModel');

const ClassData = require('./ClassDataModel');
const SportData = require('./SportDataModel');

// 导出所有模型
module.exports = {
  User,
  StudentInfo,
  ClassData,
  SportData
};